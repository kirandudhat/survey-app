import React from "react";
import "./dashboard.css";
const Dashboard = (props) => {
  return (
    <div className="dashboard">
      <p>
        No reports have been configured. To add report on the dashboard - go to
        Analytics section and then click on "Pin to Dashboard"
      </p>
    </div>
  );
};

export default Dashboard;
